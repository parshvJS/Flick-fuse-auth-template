import { useRouter } from "next/router";

const ExamPage = () => {

  // Use username and examName to fetch exam data, render UI, etc.

  return (
    <div>
      <h1>Exam Page</h1>
   
      {/* Render other components or UI elements */}
    </div>
  );
};

export default ExamPage;
